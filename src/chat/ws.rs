trait WebSocket {
    // Add functions so that a client can connect up and get updates for direct
    // messages and chatrooms.
    //
    // TODO ensure our messages that come through the pipe have enough detail so the
    // client can determine where a message should be grouped. May need to add a message
    // wrapper for this transport layer or update the existing structs to contain this meta-data
}
